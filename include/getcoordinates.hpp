#ifndef GET_COORDINATES
#define GET_COORDINATES

#include <string>

class getCoordinates
{
private:
    float longitude;
    float latitude;
public:
    getCoordinates();
    void Coordinates();
    float getLongitude();
    float getLatitude();
};

#endif